import React from 'react'

type Props = {}

const Spinner = (props: Props) => {
  return (
    <div className='animate-spin  h-10 w-10 border-4 border-t-primary-500 rounded-full'></div>
  )
}

export default Spinner